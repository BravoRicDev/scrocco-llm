"""Componenti di routing estratte da router.py (refactor incrementale, Phase 4).

[IT] Ogni modulo espone un mixin che il `Router` compone: il codice e' spostato
verbatim, nessuna modifica di comportamento.
[EN] Routing components split out of router.py. Each module exposes a mixin the
Router composes; code is moved verbatim, behaviour unchanged.
"""
